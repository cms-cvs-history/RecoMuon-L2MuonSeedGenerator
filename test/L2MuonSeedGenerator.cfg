process L2Seed  = {
    
    #----------------- input file --------------------------
    source = PoolSource{
	untracked vstring fileNames ={ 'file:runGMT.root' }
	untracked int32 maxEvents = -1
    }
    
    service = Timing { }

    include "RecoMuon/L2MuonSeedGenerator/data/L2MuonSeeds.cff"

    # Root/Pool output
    #
    module o2 = PoolOutputModule
    {
       untracked string fileName = "test.root"
    }

    
    path p = { L2MuonSeeds }
    
    #endpath e = { o2 }

}
